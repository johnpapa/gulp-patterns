var util = require('../')(['defineSrc']);
var should = require('should');
var path = require('path');
require('mocha');

describe('defineSrc', function(){
  it('should be a vinyl-source-stream instance', function(done){
    util.defineSrc.should.equal(require('vinyl-source-stream'));
    done();
  });
});