var util = require('../')(['lazypipe']);
var should = require('should');
var path = require('path');
require('mocha');

describe('lazypipe', function(){
  it('should be a lazypipe instance', function(done){
    util.lazypipe.should.equal(require('lazypipe'));
    done();
  });
});