var util = require('../')(['replaceExtension']);
var should = require('should');
var path = require('path');
require('mocha');

describe('replaceExtension', function(){
  it('should be a replace-ext instance', function(done){
    util.replaceExtension.should.equal(require('replace-ext'));
    done();
  });
});