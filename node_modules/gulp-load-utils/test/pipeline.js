var util = require('../')(['pipeline']);
var should = require('should');
var path = require('path');
require('mocha');

describe('pipeline', function(){
  it('should be a multipipe instance', function(done){
    util.pipeline.should.equal(require('multipipe'));
    done();
  });
});